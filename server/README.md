# Server-based Web Application

This directory contains a minimal Docker setup for running InfluxDB and Grafana.
Run `docker-compose up -d` to start the services. Use InfluxDB to store sensor
data from the ESP32 device and Grafana to visualize it.
