# Feature Development

1. Read `AGENTS.md` and `.agents/context-index.md`.
2. Confirm which module owns the change.
3. Update setup or status docs if runtime behavior changed.
4. Run the narrowest relevant validation available.
5. Merge through `feature -> develop -> main`.
